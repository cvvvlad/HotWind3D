<Window x:Class="HotWind3D.View.WindowProvider"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding WindowProvider,Source={StaticResource Locator}}"
        Title="WindowProvider" Height="300" Width="300">
    <Grid>
        <ContentControl Content="{Binding }"/>
    </Grid>
</Window>
